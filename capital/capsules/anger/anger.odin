package anger

// Increase strength_mul each time characters gets hurt.
// Change strength_mul type to f16, increase by 0.1 when .HURT
// Will imply some rounding...

import "../../entities"
import "../../rng"

